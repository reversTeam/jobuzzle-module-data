class DataDataTemplateMiniatureView extends ElementDataTemplateMiniatureView {

	display : function () {
		var entity = this.state.entity;
		return (
			<div>{entity.getId() +' : '+ entity.getType() +' | Url -> '+ entity.getUrl()}</div>
		);
	}

}